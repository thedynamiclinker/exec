.TH CODEX_APP_SERVER_DAEMON 1 "July 2026" "codex-cli 0.142.0" "User Commands"
.SH NAME
codex\-app\-server\-daemon \- Manage the local app\-server daemon
.SH SYNOPSIS
.EX
Usage: codex app\-server daemon [OPTIONS] <COMMAND>

.EE
.SH DESCRIPTION
Manage the local app\-server daemon
.SH COMMANDS
.TP
.B bootstrap
Install durable local app\-server management for SSH\-driven use
.TP
.B start
Start the local app server daemon if it is not already running
.TP
.B restart
Restart the local app server daemon
.TP
.B enable\-remote\-control
Enable remote control for future starts and a currently running managed daemon
.TP
.B disable\-remote\-control
Disable remote control for future starts and a currently running managed daemon
.TP
.B stop
Stop the local app server daemon
.TP
.B version
Print local CLI and running app\-server versions as JSON
.TP
.B help
Print this message or the help of the given subcommand(s)
.SH OPTIONS
.TP
.B \-c, \-\-config <key=value>
Override a configuration value that would otherwise be loaded from `~/.codex/config.toml`. Use a dotted path (`foo.bar.baz`) to override nested values. The `value` portion is parsed as TOML. If it fails to parse as TOML, the raw string is used as a literal.
.PP
Examples: \- `\-c model="o3"` \- `\-c 'sandbox_permissions=["disk\-full\-read\-access"]'` \- `\-c shell_environment_policy.inherit=all`
.TP
.B \-\-enable <FEATURE>
Enable a feature (repeatable). Equivalent to `\-c features.<name>=true`
.TP
.B \-\-disable <FEATURE>
Disable a feature (repeatable). Equivalent to `\-c features.<name>=false`
.TP
.B \-h, \-\-help
Print help (see a summary with '\-h')
.SH SEE ALSO
.BR codex (1),
.BR codex\-app\-server (1),
.BR codex\-app\-server\-daemon\-bootstrap (1),
.BR codex\-app\-server\-daemon\-start (1),
.BR codex\-app\-server\-daemon\-restart (1),
.BR codex\-app\-server\-daemon\-enable\-remote\-control (1),
.BR codex\-app\-server\-daemon\-disable\-remote\-control (1),
.BR codex\-app\-server\-daemon\-stop (1),
.BR codex\-app\-server\-daemon\-version (1)
