#!/usr/bin/env bash

owner="$1"  # user or org
prefix="${2:-.}"

git repos --verbose "$owner" | while read repo url; do
    path="$prefix/$repo"
    if [[ -d "$path" ]]; then
        continue
    fi
    git clone "$url" "$path"
done

