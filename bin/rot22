#!/usr/bin/env python

import os, sys

if os.isatty(0):
    s = ' '.join(sys.argv[1:])
else:
    s = sys.stdin.read().strip('\n')

n = 22
d = {}
for c in (65, 97):
    for i in range(26):
        d[chr(i+c)] = chr((i+n) % 26 + c)

print("".join([d.get(c, c) for c in s]))
