#!/usr/bin/env python

import os, sys

H = "אבגדהוזחטיכלמנסעפצקרשת"
s = sys.stdin.read().strip('\n')
n = len(H)
h = {}

e = len("abcdefghijklmnopqrstuvwxyz")
for н in (65, 97):
    for S in range(e):
        h[chr(S+н)] = chr((S+n) % e + н)

print("".join([h.get(н, н) for н in s]))
