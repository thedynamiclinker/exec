#!/usr/bin/env bash

cache="$HOME/.cache/lord/$PPID"

if [[ ! -e "$cache" ]]; then
    echo "The lord has not yet known this shell."
    exit 1
fi

content="$(cat "$cache")"
echo "$content"
echo "$content" | xc
