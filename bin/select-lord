#!/usr/bin/env bash

cmd=lord

while [[ -n $1 ]]; do
    case "$1" in
        -i|--yhwh)
            cmd=yhwh
            shift
            ;;
        *)
            break
            ;;
    esac
done

printf '%s\n' "$cmd"
