#!/usr/bin/env bash

set -euo pipefail

tmp="$(mktemp -d)"
trap 'rm -rf "$tmp"' EXIT

git diff --name-only "$@" | while read -r file; do
    mkdir -p "$tmp/$(dirname "$file")"
    git show "HEAD:$file" > "$tmp/$file" 2>/dev/null || true
done

meld "$tmp"
