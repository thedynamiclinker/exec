#!/usr/bin/env bash

[ -t 0 ] && {
    now="$(date +%Y%m%d-%H%M%S.%3N)"
    cache="$HOME/.cache/lord/$now"
    echo "$@" > "$cache"
    exit 0
}

[ ! -t 0 ] && {
    now="$(date +%Y%m%d-%H%M%S.%3N)"
    cache="$HOME/.cache/lord/$now"
    cat > "$cache"
    exit 0
}

